spring:
  application:
    name: java-users-simple-crud

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    enable:
      all: true
  prometheus:
    metrics:
      export:
        enabled: true


